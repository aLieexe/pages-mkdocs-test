# API Reference

## runTask(name, options)  
Executes a task with optional runtime parameters.

### Signature
```
runTask(taskName: string, options?: TaskOptions): Promise<Result>
```

## Parameters  
- `taskName`: task identifier  
- `options`: optional overrides  
